<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:avalonCompletion="clr-namespace:ICSharpCode.AvalonEdit.CodeCompletion;assembly=ICSharpCode.AvalonEdit">

    <!--
        AvalonEdit CompletionWindow / CompletionList 스타일.
        Generic.xaml에서 머지되어 모든 CodeNodeEditor 인스턴스에 자동 적용.
        VSMVVM.WPF.Design의 시맨틱 토큰을 사용하므로 다크/라이트 테마를 따라간다.
    -->

    <Style TargetType="{x:Type avalonCompletion:CompletionWindow}">
        <Setter Property="Background" Value="{DynamicResource BgSecondary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderDefault}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="WindowStyle" Value="None"/>
        <Setter Property="AllowsTransparency" Value="True"/>
        <Setter Property="ShowInTaskbar" Value="False"/>
        <Setter Property="MinWidth" Value="200"/>
        <Setter Property="MaxHeight" Value="240"/>
    </Style>

    <Style TargetType="{x:Type avalonCompletion:CompletionList}">
        <Setter Property="Background" Value="{DynamicResource BgSecondary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="FontFamily" Value="Cascadia Mono, Consolas"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>

    <!-- CompletionList 내부의 ListBox 항목 — hover/select 색을 디자인 토큰으로 강조 -->
    <Style x:Key="VsmvvmCompletionListBoxItem" TargetType="ListBoxItem">
        <Setter Property="Padding" Value="{DynamicResource Px2Py0_5}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BgTertiary}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentPrimaryMuted}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
