<UserControl x:Class="VSMVVM.WPF.Scheduler.Controls.SchedulerLogPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:runtime="clr-namespace:VSMVVM.Core.Scheduler.Runtime;assembly=VSMVVM.Core.Scheduler"
             xmlns:vm="clr-namespace:VSMVVM.WPF.Scheduler.ViewModels"
             xmlns:conv="clr-namespace:VSMVVM.WPF.Scheduler.Converters"
             d:DataContext="{d:DesignInstance Type=vm:SchedulerLogViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <conv:LogLevelToBrushConverter x:Key="LogLevelToBrush"/>
    </UserControl.Resources>

    <Border Background="{DynamicResource BgPrimary}"
            BorderBrush="{DynamicResource BorderDefault}"
            BorderThickness="0,1,0,0">
        <DockPanel>
            <Border DockPanel.Dock="Top"
                    Background="{DynamicResource BgTertiary}"
                    BorderBrush="{DynamicResource BorderDefault}"
                    BorderThickness="0,0,0,1"
                    Padding="{DynamicResource Px3Py1}">
                <TextBlock Text="Scheduler Log"
                           Foreground="{DynamicResource TextPrimary}"
                           FontWeight="Bold"/>
            </Border>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Entries}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type runtime:SchedulerLogEntry}">
                            <Border Padding="{DynamicResource Px2Py0_5}"
                                    BorderBrush="{DynamicResource BorderMuted}"
                                    BorderThickness="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Column="0" Grid.Row="0"
                                               Text="{Binding Timestamp, StringFormat={}{0:HH:mm:ss.fff}}"
                                               Foreground="{DynamicResource TextMuted}"
                                               FontFamily="Cascadia Mono, Consolas"
                                               FontSize="11"
                                               Margin="{DynamicResource Mr2}"/>
                                    <TextBlock Grid.Column="1" Grid.Row="0"
                                               Text="{Binding Level}"
                                               Foreground="{Binding Level, Converter={StaticResource LogLevelToBrush}}"
                                               FontWeight="Bold"
                                               FontFamily="Cascadia Mono, Consolas"
                                               FontSize="11"
                                               MinWidth="60"
                                               Margin="{DynamicResource Mr2}"/>
                                    <TextBlock Grid.Column="2" Grid.Row="0"
                                               Text="{Binding Message}"
                                               Foreground="{Binding Level, Converter={StaticResource LogLevelToBrush}}"
                                               TextWrapping="Wrap"
                                               FontFamily="Cascadia Mono, Consolas"
                                               FontSize="11"/>

                                    <!--
                                        Exception 이 있을 때만 두 번째 줄 — "▸ {TypeName}: {Message}".
                                        기본 Collapsed, Exception != null 이면 DataTrigger 로 Visible.
                                    -->
                                    <TextBlock Grid.Column="2" Grid.Row="1"
                                               Foreground="{DynamicResource Error}"
                                               TextWrapping="Wrap"
                                               FontFamily="Cascadia Mono, Consolas"
                                               FontSize="11"
                                               Margin="0,2,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Exception}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                        <Run Text="▸ "/>
                                        <Run Text="{Binding Exception.Message, Mode=OneWay}"/>
                                    </TextBlock>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>
